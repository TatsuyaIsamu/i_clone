<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @picture.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @picture.content %>
</p>

<p>
  <strong>Image:</strong>
  <%= @picture.image %>
</p>
<% if admin %>
 <%= link_to '編集する', edit_user_path(current_user.id) %>
 <%= link_to '削除する', user_path, method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>
<%= link_to 'Back', pictures_path %>
